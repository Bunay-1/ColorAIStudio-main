apiVersion: v1
kind: Service
metadata:
  name: {{ include "icap.fullname" . }}
  labels:
    {{- include "icap.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "icap.selectorLabels" . | nindent 4 }}
